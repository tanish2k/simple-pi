{
  "name": "simple-pi",
  "private": true,
  "workspaces": ["frontend", "server"],
  "scripts": {
    "dev": "concurrently --names \"server,frontend\" --prefix-colors \"cyan,green\" \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "dev:server": "npm run dev -w server",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w server && npm run build -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
